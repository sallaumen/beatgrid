# Build artifacts
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
/priv/plts/*.plt
/priv/plts/*.plt.hash

# Phoenix assets
/assets/node_modules/
/priv/static/assets/
/priv/static/cache_manifest.json

# ExUnit `@tag :tmp_dir` scratch dirs
/tmp/

# Local env / secrets — never commit credentials
.env
.env.*
!.env.example

# OS noise
.DS_Store

# Local-only knowledge graph (machine-scoped, never committed)
graphify-out/

# NOTE: the music library itself lives under ~/Music/DJ, NOT in this repo.
# Beatgrid manages those files on disk; the repo only holds code + a Postgres DB.
